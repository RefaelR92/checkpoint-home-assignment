# checkpoint-home-assignment

In order to manage terraform state as remote,
i've created a bucket via terraforn\/init workspaceterraform-aws-modules/vpc/aws

clarifications

1. im using single NAT gateway, but for real production enviroment its better to
   use at least two NATs in different AZ for high availability
